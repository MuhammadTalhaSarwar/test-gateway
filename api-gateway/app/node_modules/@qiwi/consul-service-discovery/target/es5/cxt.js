"use strict";
/** @module @qiwi/consul-service-discovery */
Object.defineProperty(exports, "__esModule", { value: true });
exports.createContext = void 0;
var Consul = require("consul");
var defaults_1 = require("./defaults");
var logger_1 = require("./logger");
var createContext = function (cxt) {
    if (cxt === void 0) { cxt = {}; }
    return {
        Consul: function (opts) {
            return cxt.Consul ? cxt.Consul(opts) : new Consul(opts);
        },
        Promise: cxt.Promise || Promise,
        logger: (0, logger_1.createLogger)(cxt.logger || console),
        timeout: cxt.timeout || defaults_1.DEFAULT_TIMEOUT
    };
};
exports.createContext = createContext;
