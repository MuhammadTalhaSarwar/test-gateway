import { adapter } from '../common.mjs';
import { debounce } from './debounce.mjs';
export const throttle = adapter((fn, opts) => {
    const maxWait = typeof opts.delay === 'number'
        ? opts.delay
        : undefined;
    return debounce(fn, Object.assign({ leading: true, maxWait }, opts));
});
