import { adapter } from '../common.mjs';
import { debounce } from './debounce.mjs';
export const delay = adapter((fn, opts) => {
    const delay = typeof opts.delay === 'number'
        ? { period: opts.delay, count: Number.POSITIVE_INFINITY }
        : opts.delay;
    const maxWait = typeof opts.delay === 'number'
        ? opts.delay
        : undefined;
    return debounce(fn, Object.assign(Object.assign({ maxWait }, opts), { delay, order: 'fifo' }));
});
