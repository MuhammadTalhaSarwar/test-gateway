import { adapter } from '../common.mjs';
import { debounce } from './debounce.mjs';
export const stabilize = adapter((fn, opts) => {
    const maxWait = typeof opts.delay === 'number'
        ? opts.delay
        : undefined;
    return debounce(fn, Object.assign({ maxWait }, opts));
});
